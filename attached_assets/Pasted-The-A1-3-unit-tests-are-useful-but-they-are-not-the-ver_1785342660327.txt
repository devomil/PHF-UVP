The A1-3 unit tests are useful but they are not the verification this phase requires. TEST 4 is a predicted log line, not a captured one, and TEST 1–3 exercise the functions in isolation rather than through a generation. Every bug this phase fixes was an integration bug that would have passed component-level tests — S1 was a correct optimizer given the wrong provider argument, S2 a correct formula reading a wrong default. Please produce real runtime evidence before A1-4:

A1-1 — four real generations, log lines verbatim. Normal pipeline scene (cfg=0.40 via animationStyle default (subtle-motion)); action-heavy pipeline scene (cfg=0.55 ... (dynamic)); Asset Library at max fidelity (via user fidelity slider (1), cfg=0.10); stylized character-reference scene — either the stylized-floor line, or the honest report of which provider it actually routed to. No evidence has been provided for this task at all.
A1-2 — exercise the new path. Temporarily set FINAL_MAX_WORDS to 40, run one non-stylized scene, confirm the PROMPT TRUNCATED warning fires, the retained text ends on a sentence boundary, and the prompt is not empty. Restore to 200 and confirm the restore. Do not commit the temporary value.
A1-3 — one real cinematic generation, no explicit provider. Paste the actual [AIVideo] Intelligent selector constraint: [...] line and the selector's recommendation from that run, and confirm ⚡ Runway provider blocked from auto-routing did not fire.
Check the schema enum. The constraint block sits immediately before the JSON instruction, but the schema line still enumerates runway in recommendedProvider. The permissive enum is in the more recent position and may override the constraint. In the run above, report whether Claude ever returned a Runway model. If it did, do not fix it yet — report it and stop.
Pre-enforcement numbers now. [AIVideo] Pre-enforcement: N words is pre-existing logging, so any multi-scene run produces it today. Run one project and paste every line.
Housekeeping: files and line ranges touched per task, npx tsc --noEmit and npm run build results, and confirmation that A1-1, A1-2, A1-3 were three separate commits.

Hold A1-4 until all of the above is reported.